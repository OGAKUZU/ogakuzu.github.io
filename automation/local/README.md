# ローカル運用ツール（あなたのPCで動かす道具）

Claudeが作り、**あなたのPCのローカルLLM（LM Studio）が実行する**ための道具です。
クラウド課金なし・無制限・場中いつでも使えます。

---

## セットアップ（初回だけ・5分）

### 1. LM Studioでサーバーを起動
1. LM Studioを開く
2. 左のアイコンから **Developer（`</>`）** タブを選ぶ
3. **Start Server** を押す（既定は `http://localhost:1234`）
4. モデル（Gemma 4 E4B など）が読み込まれていることを確認

> 💡 **速度改善**: 設定で **Reasoning/Thinking を OFF** にすると3〜5倍速くなります。要約作業に思考モードは不要です。

### 2. Pythonの準備
コマンドプロンプト（スタートメニューで「cmd」と検索）で：
```
pip install requests
```
※Pythonが無い場合は https://www.python.org/downloads/ からインストール（**「Add python.exe to PATH」に必ずチェック**）

### 3. kabu.py を保存
このフォルダの `kabu.py` を、分かりやすい場所（例：`C:\kabu\kabu.py`）に置く。

---

## 使い方

コマンドプロンプトで、置いた場所へ移動してから実行します。
```
cd C:\kabu
```


### まず接続確認（うまくいかない時はこれ）
```
python kabu.py --check
```
- `✅ 接続OK。利用できるモデル: ...` → 準備完了
- `⚠️ モデルが読み込まれていません` → LM Studioで **「+ Load Model」(Ctrl+L)**
- `❌ サーバーに届きません` → Developer タブで **Start Server**

### 開示・決算を解読する（いちばん使う）
```
python kabu.py
```
→ 本文を貼り付け → **Ctrl+Z を押して Enter** → 30秒ほどで解説が出ます

### 決算の「予想比」をチェックする
```
python kabu.py -m b
```
先週のディスコ（4割増益でも予想割れで急落）・信越化学（増配でも計画が予想割れで-6%）の
失敗を繰り返さないためのモードです。

### 材料の質を25点満点で採点する
```
python kabu.py -m c
```

### 用語をその場で調べる
```
python kabu.py -m d -t 逆日歩
```

### ファイルから読み込む
```
python kabu.py -f news.txt
```

---

## 🔑 EDINET連携（あなたのPCだけの特権）

**EDINET API（金融庁の開示システム）は海外IPを遮断しています。**
クラウドのClaudeからは403で使えませんが、**日本にあるあなたのPCからは使えます。**

### 準備
1. https://api.edinet-fsa.go.jp/ でAPIキーを取得（無料）
2. コマンドプロンプトで環境変数に設定（**そのウィンドウを閉じるまで有効**）：
```
set EDINET_API_KEY=あなたのキー
```
※毎回設定するのが面倒なら、Windowsの「システム環境変数の編集」で恒久設定できます

### 使う
```
python kabu.py --edinet                    # 今日の大量保有報告書を一覧表示
python kabu.py --edinet --analyze          # ＋ローカルLLMが解説
python kabu.py --edinet --date 2026-07-30  # 日付を指定
```

**なぜ重要か**: 大量保有報告書は「誰が・どの銘柄を・何%買ったか」の一次情報です。
アクティビスト（＝物言う株主）の買い増しは、ニュースになる前に分かります。
**朝のレポートより速い情報が、あなたのPCから直接取れます。**

---

## 実戦での動線

```
SBIアプリのアラートが鳴る（+5%上昇 など）
  ↓
株探やSBIで開示・記事を開く → 本文をコピー
  ↓
python kabu.py  → 貼り付け → Ctrl+Z → Enter
  ↓
30秒で「何がすごいか・注意点・予想比は分かるか」を把握
  ↓
朝レポートで決めた鉄則（損切りライン・見送り基準）で判断
```

---

## 困ったとき

| 症状 | 対処 |
|---|---|
| つながらない | LM StudioでStart Serverを押したか確認 |
| 遅い（1分以上） | Reasoning/ThinkingをOFF、Context Lengthを4096に |
| 落ちる | モデルを小さく（8B→4B）、他アプリを閉じる |
| EDINETが403 | APIキーを確認。VPNを使っていればOFFに（海外IP判定される） |
| 日本語が変 | Gemma系かQwen系のモデルに変更 |

---

## 注意

- ローカルLLMは**貼り付けた文章の中身しか知りません**。「今日の株価は？」と聞かないこと（もっともらしい嘘をつきます）
- 出力の数字は**必ず原文で確認**してください
- 本ツールの出力は公開情報の整理であり、投資助言ではありません

---

# 📡 watch.py — 一次情報リアルタイム監視（ニュースより速い）

企業が出した「原文」を、報道より先につかむための道具です。

## いちばん使う3つ

```
python watch.py                  # 今日のTDnet適時開示を一覧
python watch.py --watch --beep   # 60秒ごとに監視、重要な新着で音が鳴る（場中つけっぱなし）
python watch.py --sources        # 一次情報リンク集（どこを見ればよいか）
```

**表示の見方**
- 🔥 = 株価が動きやすい好材料（上方修正・自社株買い・増配・株式分割・TOB・受注 など）
- ⚠️ = 悪材料（下方修正・減配・特別損失・公募増資 など）
- 🆕 = 前回チェック以降の新着

## 絞り込み・応用

```
python watch.py --hot                    # 重要キーワードだけ表示
python watch.py --filter 上方修正         # 特定の言葉で絞る
python watch.py --analyze                # ローカルLLMが「なぜ株価に効くか」を解説
python watch.py --date 2026-08-05        # 過去日を指定
python watch.py --edinet                 # EDINET大量保有報告書（要APIキー）
python watch.py --debug                  # 表示が変な時：HTMLを保存して原因調査
```

## おすすめの使い方（1日の流れ）

| 時刻 | すること |
|---|---|
| 8:50 | `python watch.py` で寄り前の開示をチェック |
| 場中 | `python watch.py --watch --beep` をつけっぱなし。音が鳴ったら画面を見る |
| **15:00〜16:00** | **決算・開示のラッシュ。ここが最も価値が高い時間帯** |
| 材料発見時 | PDFを開いて原文コピー → `python kabu.py -m b` で**予想比を必ず確認** |

## 注意

- TDnetのページ構造が変わると表示が崩れることがあります。その場合は `--debug` を付けて実行し、
  保存された `tdnet_debug_p1.html` をClaudeに共有してください（すぐ直します）
- 監視間隔は既定60秒。短くしすぎるとサイトに負荷をかけるので `--interval 30` 未満は避けてください
- 開示の**PDF原文を必ず確認**してください。タイトルだけでは判断できません

---

# 🗃️ collect.py — 開示の自動収集（Pythonだけ・AI不要）

**LM Studioを起動しなくても動きます。**メモリ数十MBなのでPCは重くなりません。
「集める」のに知能は不要——AIは後で「読む」ときだけ使えば十分です。

## 使い方

```
python collect.py                 # 30分ごとに自動収集（Ctrl+Cで停止）
python collect.py --once          # 1回だけ（タスクスケジューラ向け）
python collect.py --pdf           # 重要な開示のPDFも自動保存
python collect.py --report        # 今日集めた分の要約を表示
python collect.py --interval 900  # 15分ごとにする
```

## 何が保存されるか

| ファイル | 内容 |
|---|---|
| `data/tdnet_YYYYMMDD.csv` | その日の全開示（**Excelでそのまま開けます**） |
| `data/important.csv` | **好材料・悪材料だけを日付をまたいで蓄積** |
| `pdf/` | `--pdf` 指定時のPDF（重要な開示のみ） |
| `collect.log` | 実行の記録 |

CSVには「区分」列が付きます：**好材料 / 悪材料 / 決算 / 定例 / その他**
→ Excelでフィルタすれば、後から「上方修正だけ」を一覧できます。

**同じ開示は二重に記録されません**（時刻・コード・表題で判定）。
つけっぱなしでも、何度実行してもCSVは汚れません。

## Windowsで自動起動する（タスクスケジューラ）

朝つけ忘れても勝手に動くようにする方法です。

1. スタートメニューで「**タスク スケジューラ**」を検索して開く
2. 右の「**基本タスクの作成**」→ 名前:「株の開示収集」
3. トリガー：「**毎日**」→ 開始時刻 **8:50**
4. 操作：「**プログラムの開始**」
   - プログラム: `python`
   - 引数の追加: `collect.py --once --pdf`
   - **開始（オプション）**: `C:\kabu` ← ここを忘れると動きません
5. 完了後、作成したタスクを右クリック →「プロパティ」→「**トリガー**」タブ
   → 編集 →「**繰り返し間隔: 30分**」「継続時間: 8時間」にチェック

これで **8:50〜16:50 の間、30分ごとに勝手に収集**されます。

## 🔗 Claudeに読ませる（Googleドライブ橋渡し）

**Claudeはクラウドで動いているので、あなたのPCの `C:\kabu\data` を直接は見られません。**
ただし **Googleドライブは読めます**。そこで、CSVの保存先をドライブ同期フォルダにします。

### 手順（初回だけ）

1. **Googleドライブ デスクトップ版**をインストール
   https://www.google.com/drive/download/ （無料・公式）
   → インストール後、エクスプローラーに「**マイドライブ (G:)**」等が増えます
2. マイドライブの中に **`kabu-data`** というフォルダを作る
3. `収集.bat` の中身を、保存先を指定する形に書き換える：

```bat
@echo off
cd /d C:\kabu
python collect.py --once --pdf --outdir "G:\マイドライブ\kabu-data"
```

> ドライブレターは環境で変わります。エクスプローラーで実際のパスを確認してください
> （`C:\Users\ytata\マイドライブ\kabu-data` になっている場合もあります）。

**恒久設定にするなら**、環境変数 `KABU_DATA_DIR` に同じパスを入れておけば `--outdir` は不要です。

### これで何ができるようになるか

- 昼スキャン・夕方の検証で、**Claudeが実際に集まった開示CSVを読んで**コメントできます
- 「今日の重要な開示を見て」と言うだけで、**あなたのPCが集めた一次情報**を土台に判断できます
- PDFもドライブに乗るので、原文の確認が速くなります

**保存先を変えても、既存の `C:\kabu\data` は消えません**（新しい分が新フォルダに貯まるだけ）。

---

## 1日の流れ（LLMを使わない軽量版）

| 時刻 | すること | 負荷 |
|---|---|---|
| 8:50〜16:50 | `collect.py` が自動で収集（またはタスクスケジューラ） | 軽い |
| 昼休み・夕方 | `python collect.py --report` で重要な開示だけ確認 | 軽い |
| 気になる開示があった時**だけ** | LM Studioを起動 → `kabu.py -m b` で予想比チェック | 重い（都度） |

**LM Studioは常時起動しない。これでPCは軽いままです。**

---

# ⏰ 鮮度で勝つためのタイムテーブル

「情報が速い」だけでは勝てません。**速い情報が"まだ値段に入っていない"時間帯**に使って初めて価値が出ます。
開示が出る時刻と、株が売買できる時刻のズレを利用します。

| 時刻 | 何が起きるか | あなたの動き | 鮮度の価値 |
|---|---|---|---|
| **8:50〜8:59** | 寄り前の開示がまとまって出る | `収集.bat` 実行 →「🔥」を確認 | ★★☆ 板が既に反応済み |
| 9:00〜9:15 | 寄り付き。前日材料が消化される | **見るだけ**（鉄則：寄り成り買いはしない） | ー |
| **11:30〜12:30** | **昼休み。ここが最大の狙い目** | 収集 → 🔥を探す → PDFで予想比確認 → **12:30の再開に注文** | ★★★ **市場が止まっている＝考える時間が全員に平等にある。準備した人だけが勝てる** |
| 12:30 | 後場再開 | 昼に決めた注文だけ出す | ★★★ |
| 13:00〜14:45 | 静かな時間 | 放置（収集は自動） | ★☆☆ |
| **15:00〜16:30** | **決算・開示のラッシュ。1日で最も情報量が多い** | 収集 → 🔥を仕分け → **翌朝の候補リストを作る** | ★★★ **翌日の寄りまで16時間ある＝最も準備時間が長い** |
| 16:30〜 | PTS（時間外取引）が動く | **PTS +15%超は見送り**（鉄則どおり） | ー |

## いちばん大事な考え方

**株価が動くのは「情報が出た瞬間」ではなく「取引できる瞬間」です。**

- **場中（9:00〜11:30, 12:30〜15:00）に出た開示** → 数十秒で株価に反映されます。**個人が速さで勝つのはほぼ無理**。追いかけない
- **昼休み・引け後に出た開示** → **次に取引できるまで時間がある**。ここが**準備で差がつく唯一の場所**

だから収集は場中つけっぱなしでいいですが、**あなたが本気で見るのは「昼休み」と「15時以降」の2回だけ**で十分です。

## 材料を見つけてからの手順（ここを飛ばさない）

```
🔥 を見つけた
  ↓
PDFを開く（--pdf で自動保存されています）
  ↓
「市場予想（コンセンサス）との比較」を探す ← 最重要
  ↓
書いていない／分からない  →  python kabu.py -m b で確認
  ↓
それでも不明  →  「予想比不明」として候補から外す か、枚数を減らす
  ↓
買う前に損切りラインを決める（買値-3〜5% または当日安値割れ）
```

**7月の失敗の8割は「予想比を見ずに見出しだけで判断した」ことが原因**です
（信越化学：9%増益＋増配なのに-6%／ディスコ：4割増益なのに急落）。
**鮮度が上がったぶん、この確認を飛ばしたくなる誘惑も強くなります。ここだけは省略しないでください。**

---

## 🤖 完全自動化（タスクスケジューラ）— 2026-08-25 追加

**もう `cd` も `python` も打たなくて済みます。1回登録すれば、あとは何もしません。**

### やること（初回だけ・3分）

1. このフォルダ（`C:\kabu`）に次の4ファイルが揃っていることを確認する
   - `collect.py`（元からある）
   - `収集_自動.bat`
   - `setup_task.ps1`
   - `タスク登録.bat`
2. **`タスク登録.bat` をダブルクリックする**
3. 画面の指示に従う（保存先は自動検出されます。見つからなければパスを聞かれます）

以上です。

### 登録される内容

| タスク名 | 実行時刻 | 何を拾うか |
|---|---|---|
| 株の開示収集-1_引け直後 | **平日 15:40** | 15:30の開示（決算・特別利益・株主優待） |
| 株の開示収集-2_夕方 | **平日 17:10** | **16:00〜17:00の本命帯**（自社株買い・上方修正・初配・ToSTNeT-3） |
| 株の開示収集-3_夜 | **平日 20:00** | 19:00台（大量保有・買集め行為） |
| 株の開示収集-4_深夜 | **平日 23:00** | 22:30まで（CB発行など）。翌朝5時のレポートに間に合わせる |

時刻は2026-08-24の実測（196件の開示時刻分布）にもとづいています。

### 仕組み上の保証

- **スリープ中でも自動で起きて実行します**（`WakeToRun`）
- **PCの電源が切れていた時刻の分は、次に起動したときにまとめて実行されます**（`StartWhenAvailable`）。
  夜にPCを落とす習慣でも、朝つけた時点で前夜の分が入ります
- `collect.py` は取得済みの開示を記憶しているので、**1日4回動かしても重複しません**（新着だけ追記）
- 実行の記録は `collect_auto.log` に残ります。うまくいかないときはこれを見てください

### 解除したいとき

`解除.bat` をダブルクリックしてください。

### うまく動かないとき

| collect_auto.log の中身 | 意味 | 対処 |
|---|---|---|
| `'python' は認識されていません` | PythonにPATHが通っていない | Pythonを再インストールし『Add python.exe to PATH』にチェック |
| `[警告] 保存先が指定されていません` | ドライブのパスが渡っていない | `タスク登録.bat` をもう一度実行し、パスを手入力する |
| `取得 0件` | **正常**。その時刻に新着が無かっただけ | 対処不要 |
| ログ自体が増えない | タスクが動いていない | タスクスケジューラを開き「株の開示収集-」で始まるタスクの『前回の実行結果』を見る |

---

## ⚠️ `.bat` に `chcp` を書いてはいけない（2026-08-28 実害）

タスクスケジューラの初日、4回とも実行されたのに1件も収集できていませんでした。
`collect_auto.log` に残っていたのはこれだけです。

```
'hon' は、内部コマンドまたは外部コマンド、
操作可能なプログラムまたはバッチ ファイルとして認識されていません。
```

`python` が `hon` になっています。**先頭の3文字が食われました。**

**原因**: バッチの実行中に `chcp 65001` でコードページを変えると、
cmd.exe はファイルの読み取り位置をバイト数で覚えているのに、
以降の行を別の文字コードで解釈しようとして位置がずれます。
その結果、後続行の先頭数文字が欠けます。日本語のコメントや文字列が
入っているファイルほど、ずれ幅が大きくなります。

**対策**: `.bat` は **CP932（Shift-JIS）で保存し、`chcp` は書かない。**
コマンドプロンプトの既定が CP932 なので、変換の必要がそもそもありません。

`install.ps1` は `.bat` を CP932 で保存しているので、`chcp` を消すだけで解決します。

---

## ⚠️ `.bat` は必ず CRLF で保存する（2026-09-02 実害・真の原因）

8/28に「`chcp` が原因」と診断したが、**それは間違いだった。** `chcp` を消しても
`'hon' は認識されていません`（`python` の先頭3文字が欠ける）が再現した。

**真の原因は改行コードだった。** リポジトリの `.bat` が **LFだけ**で改行されていた。

```
CR(0x0D)の数: 0
LF(0x0A)の数: 27
```

**cmd.exe はバッチファイルをバイト位置で追いながら1行ずつ実行する。**
CRが無いと位置が少しずつずれ、**行の先頭が食われる**。
`if ( ... ) else ( ... )` のような括弧ブロックの中で、ずれが最も大きくなる。

### なぜログにエラーしか残らなかったのか

`echo` の行も同じように壊れていたが、**`echo` の行は標準エラーをログに流していない**
（`>> collect_auto.log` は標準出力だけ）。一方 `python` の行には `2>&1` が付いている。
**だから壊れた `python` のエラーだけがログに残り、`===== 日付 =====` の行は
1本も出なかった。** この「日付の行が無い」が真の手がかりだった。

### 対策

- `install.ps1` はダウンロードした内容の改行を**必ず CRLF に変換**してから保存する
- リポジトリにも `.gitattributes` を置き、`*.bat` `*.ps1` を `eol=crlf` で管理する

### 教訓

**Linux上で書いたファイルをWindowsのバッチとして渡すときは、改行コードを最初に疑うこと。**
文字コード（BOM・CP932）だけでなく、**改行コードも別の失敗軸**である。
