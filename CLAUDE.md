# このリポジトリでの進め方

トイレ検索アプリと、日本株リサーチの自動化（`automation/` と `.claude/skills/jp-stock-research/`）が同居しています。

## 確認を取らずに進めること

**株のリサーチは時間との勝負です。確認で止まると、その間に相場が動きます。**
以下は**いちいち聞かずに実行**してください。

- Web検索・Webページの取得
- Googleドライブの `kabu-data` の読み取り
- 判定ボードの作成・更新・再公開（同じURL）
- PushNotification と SendUserFile
- `automation/` と `.claude/` 配下のファイル編集
- 検証台帳（`automation/ledger/history.csv`）への追記
- ブランチ `claude/jp-stock-research-automation-ww63u1` への commit と push
- 採点ルール（`references/scoring.md`）の追記・改訂

**判断に迷う要素があるときは、聞いて止まるのではなく、前提を1行で明記して進めること。**
例:「発表時刻が特定できないので、場中発表だった場合の見分け方を添えて候補に載せる」

## 必ず確認を取ること

数が少ないので、これだけ守れば十分です。

- **main へのマージ**（正式版を書き換えるため）
- **force push、`git reset --hard`**（履歴が消えるため）
- **PRの作成**（頼まれたときだけ）
- **ドライブのファイルを削除・共有すること**
- 相手が想定していない外部への発信（GitHubへのコメント投稿、メール送信など）

## 書き方

- 中学生に分かる言葉。1文を短く。**文字数を増やさない**
- 取れなかった数字は**推測で埋めず「取得不可」**と書く
- 自分のミスに気づいたら**隠さずその場で書く**

## Windows向けのファイルを渡すとき

**実機で1回通してから渡すこと。** 過去に3回連続で失敗させています。

| ハマった原因 | 対策 |
|---|---|
| PowerShellがUTF-8を誤認識 | `.ps1` は BOM 付きで保存する。または `iex` で直接実行してファイル保存を挟まない |
| GitHubのrawが約5分キャッシュされる | **コミットSHAを埋め込んだURL**を渡す（`?v=` では回避できない） |
| `.Content` の型 | PowerShell 5.1 の `Invoke-WebRequest -UseBasicParsing` は text/* を**文字列**で返す |
| `.bat` の文字化け | コマンドプロンプトの文字コード（CP932）で保存する |
